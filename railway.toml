[build]
builder = "NIXPACKS"
buildCommand = "npm install && npm run build:server && npm run build:client"

[deploy]
startCommand = "cd dist/server && node main.js"
healthcheckPath = "/api/health"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
